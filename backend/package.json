{
  "name": "queuing-system-simulation",
  "version": "1.0.0",
  "description": "Advanced Queuing System Simulation with Web GUI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "if not exist build mkdir build && g++ -std=c++11 -I./include src/*.cpp -o build/simulation.exe",
    "build-debug": "mkdir -p build && g++ -g -std=c++11 -I./include src/*.cpp -o build/simulation-debug.exe"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5"
  },
  "devDependencies": {
    "nodemon": "^3.0.1"
  },
  "keywords": [
    "queuing",
    "simulation",
    "cpp",
    "web",
    "gui"
  ],
  "author": "Seif Rashwan",
  "license": "MIT"
}
